# ── Core ML ──────────────────────────────────────────────
torch>=2.12.0
transformers>=4.43.0
datasets>=2.20.0
accelerate>=0.30.0

# ── Fine-tuning ───────────────────────────────────────────
peft>=0.11.0
bitsandbytes>=0.43.0
trl>=0.9.0
# flash-attn>=2.5.0  # commented out — install manually if needed

# ── SAE (SAFE pipeline) ───────────────────────────────────
sae-lens>=6.0.0           # pre-trained SAEs (Goodfire Llama 3.1 8B Instruct)
transformer-lens>=2.0.0   # HookedSAETransformer for activation access

# ── RAG ───────────────────────────────────────────────────
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# ── API & Chat ────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.23.0

# ── Evaluation ────────────────────────────────────────────
scikit-learn>=1.3.0       # clustering for SAFE pipeline

# ── Data & Utilities ──────────────────────────────────────
pyarrow>=14.0.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
requests>=2.31.0
python-dotenv>=1.0.0
tensorboard>=2.16.0
